┌─────────────────────┐         HTTP/REST          ┌─────────────────────┐         JPA/Hibernate        ┌───────────┐
│    Frontend         │  ──── (Axios, JSON) ────▶  │    Backend          │  ──── (Spring Data JPA) ──▶  │  MySQL    │
│  React 19 + Vite    │  ◀── (JSON Response) ────  │  Spring Boot 3.4   │  ◀── (Entity Mapping) ────  │  AWS RDS  │
│  Port: 5173         │                            │  Port: 3333         │                             └───────────┘
└─────────────────────┘                            └─────────────────────┘
                                                          │
                                                          ├──▶ AWS S3 (이미지 저장)
                                                          ├──▶ Toss Payments (결제)
                                                          ├──▶ OpenAI API (AI 챗봇)
                                                          ├──▶ Gmail SMTP (이메일)
                                                          └──▶ OAuth2 (Google, Kakao, Naver)